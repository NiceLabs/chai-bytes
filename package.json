{
  "name": "chai-bytes",
  "private": true,
  "license": "MIT",
  "scripts": {
    "deploy": "npm publish --workspace @nice-labs/chai-bytes",
    "test": "npm test --workspace chai-bytes-tests"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
